---
"apps-docs": minor
---

Update apps-docs for the new @rxova/journey-core transition-builder syntax. The docs now present choose(({ when, otherwise }) => [...]) as the preferred inline branching style and align examples with the callback-scoped helpers inside journey.transitions. See the @rxova/journey-core release notes in this release for the underlying API changes.
